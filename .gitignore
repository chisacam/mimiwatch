# 전사 결과와 내려받은 오디오. 저작물이고 용량이 크며, 각자 다시 만들 수 있습니다.
data/

# 실제 엔드포인트 설정. API 키가 평문으로 들어갑니다.
# backends.example.json 을 복사해서 쓰십시오.
backends.json

# 실측 원자료. 결론은 measurements/RESULTS.md 에 정리되어 있습니다.
# 방송 발화 내용이 그대로 들어 있으므로 저장소에 두지 않습니다.
measurements/*.json
measurements/*.log
measurements/*.err
measurements/*.txt

__pycache__/
*.pyc
.DS_Store
